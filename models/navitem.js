class NavItem {
  constructor(id, title, bgColor, icon, target) {
    this.id = id;
    this.title = title;
    this.bgColor = bgColor;
    this.icon = icon;
    this.target = target;
  }
}

export default NavItem;
